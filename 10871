#include <iostream>
using namespace std;

int main(){
    int N,X;
    int array[N-1];
    
    cin >> N >> X;
    for (int i=0; N > i; i++){
        cin >> array[i] ;
    }
    for (int i=0; N > i; i++){
        if ( array[i] < X){
            cout << array[i] << " " ;
        }
    }
}
