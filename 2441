#include <iostream>
using namespace std;

int main(){
    int N;
    cin >> N;
    int Num = N;
        
    for (int i=0; i < N; i++){
        cout << string(i, ' ') << string(Num, '*') << endl;
        Num--;
    }
}
