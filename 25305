#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    
    int N, k;
    
    cin >> N >> k ;
    
    int nums[N];
    
    for (int i = 0; i < N;i++){
        cin >> nums[i];
    }
    
    sort(nums, nums+N, greater<int>());
    
    cout << nums[k-1];
    
}
