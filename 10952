#include <iostream>
using namespace std;

int main(){
    int A, B;
    
    while (true){
        cin >> A >> B ;
        if (A == 0 && B == 0){  //if문 블럭을 cout이후에 적어서 0 0의 값이 0으로 나와 처음에 틀렸음. 
            break;
        }
        cout << A + B << endl;

    }
    
}
