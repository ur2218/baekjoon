#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    
    int N;
    cin >> N;
    int A[10];
    
    for (int i = 0; i < N; i++){
        for (int j = 0; j < 10; j++){
            cin >> A[j];
        }
        sort(A,A+10);
        cout << A[7] << endl;
    }
  
}
